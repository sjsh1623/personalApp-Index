import {SafeAreaView, Text} from "react-native";

const LoginScreen = () => {
    return(
        <SafeAreaView >
            <Text>Login</Text>
        </SafeAreaView>
    )
}

export default LoginScreen;
