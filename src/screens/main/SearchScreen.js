import {SafeAreaView, Text} from "react-native";

const SearchScreen = () => {
    return(
        <SafeAreaView>
            <Text>SearchScreen</Text>
        </SafeAreaView>
    )
}

export default SearchScreen;
