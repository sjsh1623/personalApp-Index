import {StyleSheet} from "react-native";

const HomeStyle = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        alignItems: 'center',
    }
})

export default HomeStyle;
